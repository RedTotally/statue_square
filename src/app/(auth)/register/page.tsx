import Register from "./content";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Register | Statue Square",
  description:
    "",
};

export default function LogInPage(){
    return(
        <Register></Register>
    )
}