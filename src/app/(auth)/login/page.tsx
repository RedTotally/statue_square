import LogIn from "./content";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Log In | Statue Square",
  description:
    "",
};

export default function LogInPage(){
    return(
        <LogIn></LogIn>
    )
}